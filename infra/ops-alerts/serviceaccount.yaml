# One SA for this one actor (AR27: RBAC scales with actors, not services). The CronJob pod runs
# as this SA; its read-only ClusterRole (rbac.yaml) is the poller's only cluster access.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ops-alerter
  namespace: ops-alerts
